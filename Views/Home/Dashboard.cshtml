@{
    ViewData["Title"] = "Dashboard";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.2/css/all.min.css">
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
    <script src="/Views/Home/AjaxJS.js"></script>
    <link rel="stylesheet" href="/Views/Home/DashboardCss.css">
    
        <!DOCTYPE html>
<html lang="en">
</head>
    <script>
        function addData() {
    fetch('/api/Data/AddData', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json'
        },
        body: JSON.stringify({ name: 'Нові дані' }) // Приклад даних для додавання
    })
    .then(response => response.json())
    .then(result => {
        alert(result.message);
        // Опціонально: оновіть дані на іншій сторінці
        updateOtherPage();
    })
    .catch(error => console.error('Помилка:', error));
}

function updateOtherPage() {
    // Реалізуйте оновлення даних на іншій сторінці
    // Наприклад, викликайте інший AJAX-запит для отримання оновлених даних
}

    </script>
   
   <style>
    .body {
        margin: 0;
    }

    
    <!--navbar style -->
    .navbar-dark .navbar-toggler-icon {
        border-color: #fff;
    }

    .submenu {
        display: none;
        position: absolute;
        background-color: #f9f9f9;
        min-width: 160px;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
        z-index: 1;
    }

    .nav-item:hover .submenu {
        display: block;
    }

    .submenu a {
        color: #333;
        padding: 8px 12px;
        text-decoration: none;
        display: block;
    }

    .submenu a:hover {
        background-color: #ddd;
    }

    
    <!-- Filter selecting box -->

    body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f4;
        display: flex;
        align-items: center;
        justify-content: center;
        height: 100vh;
        margin: 0;
    }
    
    .body-container {
        padding-left: 0 !important;
        padding-right: 0 !important;
        max-width: 100%;
    }

    

    .filter-container {
        margin: 0 auto; 
        max-width: 300px;
        background-color: #dad0d0;
        padding: 20px;
        border-radius: 5px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        text-align: center;
        float:left;
    }
    .login-container {
        float:left;
        text-align: center;
    }
    .form-group {
        margin-bottom: 20px;
    }

    .form-group label {
        display: block;
        margin-bottom: 5px;
    }

    .form-group input {
        padding: 8px;
        box-sizing: border-box;
        border: 1px solid #ccc;
        border-radius: 5px;
    }

    .form-group button {
        width: 50%;
        padding: 10px;
        background-color: #484141;
        color: #fff;
        border: none;
        border-radius: 5px;
        cursor: pointer;
    }

    .FromTo, .ToFrom {
        width: 70px;
        margin-right:20px;
        margin-left: 20px;
    }
    
    
</style>
    

<body>

    
   

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="#">DefaultShop</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mr-auto">
                <!-- Left side links -->
            </ul>
            <ul class="navbar-nav ml-auto">
                <!-- Right side links -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
                       aria-haspopup="true" aria-expanded="false">
                        Administrator
                    </a>
                    <div class="dropdown-menu submenu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="#">Product Categories</a>
                        <a class="dropdown-item" href="#">Products</a>
                    </div>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Out</a>
                </li>
            </ul>
        </div>
    </nav>

    <!-- фільтр селектор -->
  
    <div class="text-center">
      <h3 class="display-4">Product List</h3>
    </div>

  <div class="filter-container">
     <h3>Filter</h3>
     <form action="#" method="get">
        <div class="form-group">
            <label for="productName">Search by Name:</label>
            <input type="text" id="productName" name="productName">
        </div>
        <div class="form-group">
            <label for="category">Search by Category:</label>
            <input type="text" id="category" name="category">
        </div>
        <div class="form-group">
            <div class="search-container">
                <label for="price">Price:</label>
                <input type="text" id="priceFrom" name="priceFrom" placeholder="From" class = "FromTo">
                <input type="text" id="priceTo" name="priceTo" placeholder="To" size="4" class = "ToFrom">
            </div>
        </div>
        <div class="form-group">
            <button type="submit">Search</button>
        </div>
    </form>
  </div>
 
 
  <!-- додавання позицій -->
  <div class = "AddButton">
    <button onclick="addData()">Додати дані</button>
  </div>
  

</body>

</html>

